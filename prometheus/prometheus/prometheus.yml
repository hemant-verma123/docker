global:
  scrape_interval: 30s
  scrape_timeout: 15s

scrape_configs:
  - job_name: 'redfish_exporter'
    scrape_timeout: 15s
    metrics_path: /metrics
    static_configs:
      - targets:
          - host.docker.internal:8000
        labels:
          instance: "redfish-localhost"
